<!DOCTYPE html>
<html>
  <head>
    <title>Sample</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <!-- scssやBootstrapをインストールできるようにしたら移動 -->
  <style>
    .sample-body{
      background-color: #000000; 
      color: #ffffff;
      padding: 1rem;
    }

    .title-text{
      text-align: center;
    }
  </style>

  <body class = "sample-body">
    <%= yield %>
  </body>
</html>
